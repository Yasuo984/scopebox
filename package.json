{
  "name": "scopebox-webmcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A WebMCP prototype where the agent can only touch what the human frames.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/domain.js && node --check src/store.js && node --check src/webmcp.js && node --check src/main.js && node --check src/review-identity.js && node --check src/review-ui-binding.js && node --check src/bootstrap.js && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
